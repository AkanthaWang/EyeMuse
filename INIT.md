# INIT.md

## 项目名称

EyeMuse

## 项目目标

打造一款多模态情绪感知桌面宠物，通过摄像头、语音、文本输入与大语言模型推理，持续感知用户状态，并以动画、语音和交互行为给出更自然、更有共情的回应。

## 项目背景

EyeMuse 关注的不是传统 AI 助手那种“被动问答”，而是更接近桌面陪伴式交互：它可以观察用户面部与疲劳状态，结合语音和文本输入理解上下文，再用 LLM 生成合适的回应。

本项目重点探索以下方向：

- 摄像头驱动的面部与疲劳感知
- 语音、文本与视觉信息的多模态融合
- 桌面宠物形象与情绪反馈
- 本地优先的数据保存与隐私控制
- 可扩展的 LLM 交互与后续能力扩展

## 核心约束

1. 交互体验优先：宠物形象、反馈节奏和状态提示要清晰、自然、不过度打扰。
2. 本地优先：尽量只保留本地数据，不主动上传用户隐私信息。
3. 隐私透明：摄像头与语音能力必须明确告知用户，并允许用户控制开关。
4. 平台中立：优先保证 Windows，可按条件扩展到 macOS 与 Linux。
5. LLM 中立：支持 OpenAI-compatible 接口，避免绑定单一模型服务。
6. 小步迭代：每个阶段保持应用可运行、可验证、可演示。
7. 留痕清晰：关键 Prompt、模型配置、验证结果与人工审核过程应保留记录。

## 功能范围

### P0：核心必需能力

- 桌面宠物基础窗口与常驻展示
- 摄像头接入与开关控制
- 面部检测与基础疲劳状态识别
- 文本输入与回复展示
- 语音输入或语音反馈的最小可用链路
- LLM 对话与情绪理解的基础接入
- 本地状态、配置和历史记录保存
- 基础错误提示、权限提示和加载状态

### P1：体验增强能力

- 更丰富的宠物动画与表情状态切换
- 多轮上下文与个性化回应
- 更稳定的疲劳检测阈值与提示策略
- 语音唤醒、TTS 或更自然的语音反馈
- 可配置的情绪风格、提醒频率与静默时段
- 基础设置页与引导页
- 使用说明与演示文档

### P2：加分能力

- 更细粒度的多模态状态融合
- 用户习惯记录与长期状态趋势
- 可切换的 LLM Provider 和模型配置
- 事件日志、调试信息和简单统计面板
- 更多桌面宠物形态、动作和主题皮肤

## 暂不考虑的功能

- 用户账号系统
- 云同步
- 远程协同或多人共享状态
- 服务端数据库
- 主动上传原始摄像头数据或音频数据
- 面向商业分发的复杂签名、商店上架和全平台正式维护

## 技术选型

- 界面层：PySide6 或 PyQt6
- 业务层：Python 本地逻辑模块
- 多模态输入：摄像头、麦克风、文本输入
- 模型接入：OpenAI-compatible LLM API
- 本地存储：JSON / SQLite 等本地文件方案
- 宠物动画：帧数据驱动或轻量动画资源
- 文档与协作：README、INIT、PLAN、Issue、PR、Release、Agent logs

## 架构原则

1. 界面与业务分层清晰：Qt 负责窗口、交互和展示，Python 业务模块负责感知、状态管理、数据存储和模型调用。
2. 隐私最小化：只收集完成功能所需的最少信息，默认本地处理。
3. 状态驱动：宠物表情、提醒与回应都应由统一状态模型驱动，避免分散逻辑。
4. 可验证性优先：所有关键功能都要能通过手工验收或脚本验证。
5. 可追踪：重要技术决策、Prompt 和验证记录应写入文档。
6. 迭代收敛：优先完成稳定主链路，再逐步增加情绪、动画和智能能力。

## 当前状态

- README 已明确 EyeMuse 的产品方向：多模态桌面宠物、摄像头疲劳检测、语音 / 文本交互与 LLM 推理。
- 当前文档的目标是把项目从概念说明收敛为可执行的阶段计划与验收清单。
- 后续推进应优先围绕桌面宠物主链路、隐私控制、模型接入和体验验证展开。

## 已知风险

1. 摄像头与麦克风权限在不同平台上的表现不一致，需要提前处理提示和容错。
2. 疲劳识别和情绪判断可能受光照、姿态、设备性能和摄像头质量影响。
3. 语音识别、TTS 和 LLM 调用都依赖外部能力，存在网络与服务稳定性风险。
4. 多模态融合如果设计过重，可能导致桌面宠物卡顿或响应过慢。
5. 宠物动画与状态机如果耦合过深，后续扩展会变复杂。

## 应对策略

1. 先建立稳定的基础状态机，再逐步叠加识别、语音和 LLM 能力。
2. 对摄像头、麦克风和模型调用都提供清晰的失败提示和降级路径。
3. 优先保证轻量、连续的桌面体验，避免高频阻塞主线程。
4. 将配置、历史记录和验证过程持续记录到文档中，保持可追踪。
5. 每个阶段完成后同步更新 PLAN.md，确保计划和现状一致。